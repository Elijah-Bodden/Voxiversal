import { Page } from 'web/components/layout/page'

export default function Index() {
  return (
    <Page>
      <div className="flex h-full flex-col items-center justify-center">
        <h1 className="text-ink-700 text-4xl">Index placeholder (webflow html will substitute (maybe just refactor into jsx? Could ensure consistent styling, etc.))</h1>
      </div>
    </Page>
  )
}